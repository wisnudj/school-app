<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=title%></title>
  </head>
  <body>
    <h1><%=title%></h1>
    <p>First Name: <%=dataStudent[0].first_name%></p>
    <p>Last Name: <%=dataStudent[0].last_name%></p>
    <p>Email: <%=dataStudent[0].email%></p>
    <p>Choose Subject</p>
    <form action="/student/<%=dataStudent[0].id%>/addsubject" method="post">
      <select name="SubjectId">
        <%for(var i = 0; i < dataSubject.length; i++) {%>
          <option value="<%=dataSubject[i].id%>"><%=dataSubject[i].subject_name%></option>
        <%}%>
      </select>
      <p><input type="submit" value="submit"></p>
    </form>
  </body>
</html>
